<%= form_for(@cuotum) do |f| %>
  <% if @cuotum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuotum.errors.count, "error") %> prohibited this cuotum from being saved:</h2>

      <ul>
      <% @cuotum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :estado %><br />
    <%= f.text_field :estado %>
  </div>
  <div class="field">
    <%= f.label :liquidacion %><br />
    <%= f.text_field :liquidacion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
