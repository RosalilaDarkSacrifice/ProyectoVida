<%= form_for(@permisos_por_rol) do |f| %>
  <% if @permisos_por_rol.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@permisos_por_rol.errors.count, "error") %> prohibited this permisos_por_rol from being saved:</h2>

      <ul>
      <% @permisos_por_rol.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rol_id %><br />
    <%= f.number_field :rol_id %>
  </div>
  <div class="field">
    <%= f.label :permiso_id %><br />
    <%= f.number_field :permiso_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
